name: qy_player
description: A new Flutter project.
version: 0.0.1
homepage: https://doc.wholeally.com/

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.ttxs.qy_player
        pluginClass: QYPlayerPlugin
      ios:
        pluginClass: QYPlayerPlugin

